# Convention doc naming

> Relocated from CLAUDE.md for context-budget; loaded on demand.

- Convention doc naming follows the build-prompt directive when explicit (e.g., `docs/architecture/feature-flags.md` per build-prompt §P1.6 line 1229); falls back to the `<concept>-convention.md` workspace pattern (`audit-event-convention.md`, `quotas-compute-placement-convention.md`, etc.) otherwise.
