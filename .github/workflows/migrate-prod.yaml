name: migrate-prod

# Manual only per ADR-CI-001 Decision 3. Schema changes are deliberate
# operator decisions, not merge side effects.
on:
  workflow_dispatch: {}

# WIF requires id-token: write. contents: read for checkout.
permissions:
  id-token: write
  contents: read

# One concurrency group per env — prevents two operators from accidentally
# racing a double apply. cancel-in-progress: false keeps a running migration
# from being killed mid-flight; queued dispatches can still be cancelled.
concurrency:
  group: migrate-prod
  cancel-in-progress: false

jobs:
  migrate:
    name: Apply migrations to prod via Cloud Build
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout repo (for migrate.yaml + source tarball)
        uses: actions/checkout@v4

      - name: Authenticate to GCP via WIF
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: projects/242079866727/locations/global/workloadIdentityPools/cortex-github-pool/providers/cortex-github-provider
          service_account: cortex-ci-submit-prod@sevyn8-cortex-prod.iam.gserviceaccount.com

      - name: Set up gcloud CLI
        uses: google-github-actions/setup-gcloud@v2

      - name: Submit Cloud Build
        run: |
          gcloud builds submit . \
            --region=asia-south1 \
            --config=infra/cloud-build/migrate.yaml \
            --service-account=projects/sevyn8-cortex-prod/serviceAccounts/cortex-ci-migration-prod@sevyn8-cortex-prod.iam.gserviceaccount.com \
            --project=sevyn8-cortex-prod \
            --substitutions=_ENV=prod,_INSTANCE_CONNECTION_NAME=sevyn8-cortex-prod:asia-south1:cortex-prod-postgres,_SECRET_NAME=cortex-db-postgres-break-glass-prod

      - name: Print build console link
        if: always()
        run: |
          echo "View build logs at:"
          echo "https://console.cloud.google.com/cloud-build/builds?project=sevyn8-cortex-prod"
