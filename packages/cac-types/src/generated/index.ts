/**
 * Barrel for types generated from the contracts repo schemas.
 *
 * Intentionally empty until codegen is wired. The contracts repo does not exist
 * yet (created in Phase R per docs/spec/v3/plan.md); the canonical contracts
 * (C1 machine-auth token, C2 spine event envelope, C3 action ledger, C4 merge,
 * C5 consent, C6 pack) are owned there. See `./README.md`.
 */
export {};
